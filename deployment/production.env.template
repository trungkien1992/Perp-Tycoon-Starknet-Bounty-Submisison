# Production Environment Configuration Template
# DO NOT commit actual production values to version control
# Use your deployment platform's secret management system

# Extended API Configuration
EXTENDED_API_KEY=REPLACE_WITH_PRODUCTION_API_KEY
EXTENDED_VAULT_ID=REPLACE_WITH_PRODUCTION_VAULT_ID
EXTENDED_API_BASE_URL=https://extended.exchange/api
EXTENDED_API_WS_URL=wss://extended.exchange/ws

# Starknet Configuration
STARKNET_PRIVATE_KEY=REPLACE_WITH_PRODUCTION_PRIVATE_KEY
STARKNET_PUBLIC_KEY=REPLACE_WITH_PRODUCTION_PUBLIC_KEY

# App Configuration
APP_MODE=real
FLUTTER_ENV=production